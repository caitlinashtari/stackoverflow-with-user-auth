<h2>New Question</h2>

<%= render "layouts/errors", :object => @question %>

<%= form_for [current_user,@question] do |f| %>

  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.label :description %>
  <%= f.text_field :description %>
  <%= f.submit  %>
<% end %>
